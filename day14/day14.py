# here is the plan
# for each element, we have a recipe that can produce that element
# the goal is to build up, for each element, the minimum fuel cost to build that element
# for each element, we will know minimum fuel cost to build that element
